using UnityEngine;

public class PlanetController : MonoBehaviour
{
    /*
    private void OnTriggerEnter(Collider other)
    {
        DummyScrip_mario playerController = other.GetComponent<DummyScrip_mario>();
        if (playerController != null)
        {
            if (playerController.currentPlanet != null)
            {
                // skip if already on this planet 
                if (playerController.currentPlanet == transform) return;

                // Attach to new planet 
                playerController.currentPlanet = transform;
                playerController.EnterNewGravityField();
            }
            if(playerController.currentPlanet == null)
            {
               
                playerController.currentPlanet = transform;
                playerController.EnterNewGravityField();
            }
        }
    }
    
  */
}